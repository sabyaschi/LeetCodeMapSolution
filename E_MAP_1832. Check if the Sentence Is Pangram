class Solution {
    public boolean checkIfPangram(String sentence) {
        HashMap<Character,Integer> mp1 = new HashMap<>();
        for(int i = 0;i<sentence.length();i++){
            mp1.put(sentence.charAt(i),1);
        }

        return mp1.size() == 26 ? true : false;
        

    }
}
